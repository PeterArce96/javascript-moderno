// Estructuras de control - Mayor y menor que

const dinero = 300;
const totalAPagar = 300;

// Mayor o igual, porque si tenemos justo 300, si podemos pagar
if (dinero >= totalAPagar) {
    console.log("Si podemos pagar")
} else {
    console.log("Fondos insuficientes");
}