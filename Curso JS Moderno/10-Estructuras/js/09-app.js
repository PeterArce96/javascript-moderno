// Estructuras - Operador ternario

const autenticado = true;
const puedePagar = false;

console.log(autenticado ? puedePagar ? 'Si está autenticado y puede pagar': 'Si autenticado, pero no puede pagar': 'No no está autenticado');

// ternario anidado, como un if dentro de otro if