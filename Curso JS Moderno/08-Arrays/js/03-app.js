// ARRAYS - Recorrer un array

const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio'];
console.table(meses);

// .length --> Medida del arreglo
console.log(meses.length);

// Recorrer el array con FOR
for(let i = 0; i < meses.length; i++ ) {
    console.log(meses[i]);
}
