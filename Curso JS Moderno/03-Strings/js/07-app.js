// Strings - toLowerCase, toUpperCase
const producto = 'Monitor 20 Pulgadas';

// toUpperCase() --> Convierte el string a Mayúsculas
console.log(producto.toUpperCase());

// toLowerCase() --> Convierte el string a Minúsuculas
console.log(producto.toLowerCase());

// Para proyectos
const email = "CORREO@CORREO.COM"
console.log(email.toLowerCase());

const precio = 300;
// Si ves el resultado de consola en color azul, es tipo Number
console.log(precio);
// toString() --> Para convertir a String
console.log(precio.toString());